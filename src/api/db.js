// put here the code to initialize your database and funtions to interact with it
const initDB = async(client = null) => {
    console.log('Initializing database...')

    console.log('Database initialized.')
}

module.exports = {
    initDB
}